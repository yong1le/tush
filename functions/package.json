{
  "name": "functions",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "sharp": "mkdir -p dist && cd dist && npm install sharp --no-save --no-package-lock",
    "build": "npm run sharp && esbuild index.ts --bundle --minify --sourcemap --platform=node --external:sharp --target=es2020 --outfile=dist/index.js --watch=forever"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.146",
    "esbuild": "^0.24.2"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0",
    "jszip": "^3.10.1",
    "sharp": "^0.33.5"
  }
}
